/**
 * Rows
 * @type {Array}
 */
export const RANKS = Object.freeze(['8', '7', '6', '5', '4', '3', '2', '1'])

/**
 * Columns
 * @type {Array}
 */
export const FILES = Object.freeze(['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'])
